{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Собрать проект C++",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/diagonal_program.cpp",
                "-o",
                "${workspaceFolder}/diagonal_program.out"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}