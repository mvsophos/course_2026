{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Собрать проект C++",
            "type": "shell",
            "command": "g++",
            "args": ["-g", "${workspaceFolder}/another_program.cpp", "-o", "${workspaceFolder}/another_program.out"],
            "group": {
                    "kind": "build",
                    "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
